# 扩展行动参与数据 - 可视化流程图

## 🎯 整体架构

```
┌──────────────────────────────────────────────────────────────────┐
│                          组件层 (UI)                              │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ 行动详情页面  │  │ 参与统计卡片  │  │ 我的参与列表  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└──────────────────────────────────────────────────────────────────┘
                              ↓ 使用
┌──────────────────────────────────────────────────────────────────┐
│                    统一数据层 (Unified Hook)                      │
│                                                                  │
│              ┌─────────────────────────────────┐                │
│              │  useActionParticipationData     │                │
│              │                                 │                │
│              │  输入: tokenAddress, actionId   │                │
│              │  输出: 统一的参与数据            │                │
│              └─────────────────────────────────┘                │
└──────────────────────────────────────────────────────────────────┘
                              ↓ 调用
┌──────────────────────────────────────────────────────────────────┐
│                      判断层 (Decision Layer)                      │
│                                                                  │
│        ┌─────────────────────────────────────────┐              │
│        │ ExtensionCenter.extension(token, id)    │              │
│        └─────────────────────────────────────────┘              │
│                          ↓                                       │
│                 返回扩展合约地址?                                  │
│                          ↓                                       │
│            ┌─────────────┴─────────────┐                        │
│            ↓                           ↓                         │
│     [零地址 0x000...]            [非零地址]                       │
│         普通行动                   扩展行动                        │
└──────────────────────────────────────────────────────────────────┘
            ↓                           ↓
┌───────────────────────┐    ┌──────────────────────────────────┐
│  核心数据源            │    │  扩展数据源                        │
│  (Core Source)        │    │  (Extension Source)              │
│                       │    │                                  │
│  LOVE20Join 合约      │    │  ┌────────────────────────────┐ │
│  - numOfAccounts()    │    │  │ useActionExtensionStats    │ │
│  - amountByActionId() │    │  │ ┌────────────────────────┐ │ │
│  - amount...ByAccount │    │  │ │ accountsCount()        │ │ │
│                       │    │  │ │ joinedValue()          │ │ │
│  使用 coreData 回退    │    │  │ └────────────────────────┘ │ │
│                       │    │  └────────────────────────────┘ │
│                       │    │                                  │
│                       │    │  ┌────────────────────────────┐ │
│                       │    │  │useActionExtensionUserStatus│ │
│                       │    │  │ ┌────────────────────────┐ │ │
│                       │    │  │ │joinedValueByAccount()  │ │ │
│                       │    │  │ │isAccountJoined()       │ │ │
│                       │    │  │ └────────────────────────┘ │ │
│                       │    │  └────────────────────────────┘ │
└───────────────────────┘    └──────────────────────────────────┘
            ↓                           ↓
            └───────────────┬───────────┘
                            ↓
┌──────────────────────────────────────────────────────────────────┐
│                      统一输出格式                                  │
│                                                                  │
│  {                                                               │
│    isExtensionAction: boolean,        // 行动类型                │
│    extensionAddress: `0x${string}`,   // 扩展合约地址             │
│    participantCount: bigint,          // 参与人数                │
│    totalAmount: bigint,               // 参与总额                │
│    userJoinedAmount: bigint,          // 用户参与金额             │
│    isJoined: boolean,                 // 用户是否参与             │
│  }                                                               │
└──────────────────────────────────────────────────────────────────┘
```

## 🔄 数据流转详细流程

### 场景 A：普通行动

```
┌─────────────┐
│  组件调用    │ useActionParticipationData(tokenAddress, actionId, account, coreData)
└──────┬──────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 1: 检查行动类型                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━│
│  useExtension(tokenAddress, actionId)                  │
│    ↓                                                   │
│  返回: 0x0000000000000000000000000000000000000000     │
│    ↓                                                   │
│  判断: isExtensionAction = false                       │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 2: 使用 Core 数据                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━│
│  participantCount = coreData.participantCount          │
│  totalAmount = coreData.totalAmount                    │
│  userJoinedAmount = coreData.userJoinedAmount          │
│  isJoined = coreData.isJoined                          │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 3: 返回统一数据                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━│
│  {                                                      │
│    isExtensionAction: false,                           │
│    extensionAddress: undefined,                        │
│    participantCount: 100n,    // from coreData         │
│    totalAmount: 1000000n,     // from coreData         │
│    userJoinedAmount: 5000n,   // from coreData         │
│    isJoined: true,            // from coreData         │
│  }                                                      │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────┐
│  组件渲染    │
└─────────────┘
```

### 场景 B：扩展行动

```
┌─────────────┐
│  组件调用    │ useActionParticipationData(tokenAddress, actionId, account)
└──────┬──────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 1: 检查行动类型                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━│
│  useExtension(tokenAddress, actionId)                  │
│    ↓                                                   │
│  返回: 0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb     │
│    ↓                                                   │
│  判断: isExtensionAction = true                        │
└─────────────────────────────────────────────────────────┘
       │
       ├──────────────────────────┬─────────────────────────┐
       ↓                          ↓                         ↓
┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐
│  步骤 2a: 获取统计   │  │  步骤 2b: 获取用户   │  │  步骤 2c: 判断参与   │
│  ━━━━━━━━━━━━━━━━  │  │  ━━━━━━━━━━━━━━━━  │  │  ━━━━━━━━━━━━━━━━  │
│  useActionExtension  │  │  useActionExtension  │  │  ExtensionCenter.   │
│  Stats               │  │  UserStatus          │  │  isAccountJoined    │
│    ↓                 │  │    ↓                 │  │    ↓                │
│  批量调用:           │  │  批量调用:           │  │  调用:              │
│  ┌─────────────────┐│  │  ┌─────────────────┐│  │  ┌─────────────────┐│
│  │accountsCount()  ││  │  │joinedValueBy    ││  │  │isAccountJoined()││
│  │                 ││  │  │Account()        ││  │  │                 ││
│  │返回: 50n        ││  │  │                 ││  │  │返回: true       ││
│  └─────────────────┘│  │  │返回: 10000n     ││  │  └─────────────────┘│
│  ┌─────────────────┐│  │  └─────────────────┘│  │                     │
│  │joinedValue()    ││  │                     │  │                     │
│  │                 ││  │                     │  │                     │
│  │返回: 500000n    ││  │                     │  │                     │
│  └─────────────────┘│  │                     │  │                     │
└─────────────────────┘  └─────────────────────┘  └─────────────────────┘
       │                          │                         │
       └──────────────────────────┴─────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 3: 整合数据                                        │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━│
│  participantCount = extensionParticipantCount (50n)    │
│  totalAmount = extensionTotalAmount (500000n)          │
│  userJoinedAmount = extensionUserJoinedAmount (10000n) │
│  isJoined = extensionIsJoined (true)                   │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 4: 返回统一数据                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━│
│  {                                                      │
│    isExtensionAction: true,                            │
│    extensionAddress: '0x742d...',                      │
│    participantCount: 50n,      // from extension       │
│    totalAmount: 500000n,       // from extension       │
│    userJoinedAmount: 10000n,   // from extension       │
│    isJoined: true,             // from extension       │
│  }                                                      │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────┐
│  组件渲染    │
└─────────────┘
```

## 📊 批量 RPC 调用优化

### 优化前（逐个调用）

```
┌────────────────────────────────────────────┐
│  扩展行动数据获取（优化前）                 │
└────────────────────────────────────────────┘

RPC Call 1: accountsCount()           ──→ [网络延迟 100ms]
              ↓ 等待响应
RPC Call 2: joinedValue()             ──→ [网络延迟 100ms]
              ↓ 等待响应
RPC Call 3: joinedValueByAccount()    ──→ [网络延迟 100ms]
              ↓ 等待响应
RPC Call 4: isAccountJoined()         ──→ [网络延迟 100ms]
              ↓ 等待响应

总耗时: ~400ms + 处理时间
总调用: 4 次 RPC
```

### 优化后（批量调用）

```
┌────────────────────────────────────────────┐
│  扩展行动数据获取（优化后）                 │
└────────────────────────────────────────────┘

批量调用组 1 (useActionExtensionStats):
  - accountsCount()
  - joinedValue()                     ──→ [网络延迟 100ms]
              ↓ 并行处理

批量调用组 2 (useActionExtensionUserStatus):
  - joinedValueByAccount()
  - isAccountJoined()                 ──→ [网络延迟 100ms]
              ↓ 并行处理

总耗时: ~200ms + 处理时间
总调用: 2 次 RPC (批量)
性能提升: 50% ⚡
```

## 🔀 集成到 useActionCoreData

### 集成前后对比

```
┌─────────────────────────────────────────────────────────────┐
│  集成前: useActionCoreData                                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  输入: tokenAddress, actionId, account                      │
│    ↓                                                        │
│  [批量调用 LOVE20Join 合约]                                  │
│    ↓                                                        │
│  输出: {                                                     │
│    actionInfo,                                              │
│    participantCount,   ← 仅支持普通行动                      │
│    totalAmount,        ← 仅支持普通行动                      │
│    userJoinedAmount,   ← 仅支持普通行动                      │
│    isJoined,           ← 仅支持普通行动                      │
│    currentRound,                                            │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘

                          ↓ 集成

┌─────────────────────────────────────────────────────────────┐
│  集成后: useActionCoreData (增强版)                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  输入: tokenAddress, actionId, account                      │
│    ↓                                                        │
│  [批量调用 LOVE20Join 合约]                                  │
│    ↓                                                        │
│  coreData = { ... }                                         │
│    ↓                                                        │
│  [调用 useActionParticipationData]                          │
│    ├─→ 自动判断行动类型                                      │
│    ├─→ 如果扩展: 从扩展合约获取                               │
│    └─→ 如果普通: 使用 coreData                               │
│    ↓                                                        │
│  输出: {                                                     │
│    actionInfo,                                              │
│    participantCount,   ← 自动支持扩展行动 ✨                  │
│    totalAmount,        ← 自动支持扩展行动 ✨                  │
│    userJoinedAmount,   ← 自动支持扩展行动 ✨                  │
│    isJoined,           ← 自动支持扩展行动 ✨                  │
│    currentRound,                                            │
│    isExtensionAction,  ← 新增 ✨                             │
│    extensionAddress,   ← 新增 ✨                             │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
```

## 🎨 组件使用示例流程

```
┌───────────────────────────────────────────────────────────┐
│  ActionDetailPage 组件                                     │
└───────────────────────────────────────────────────────────┘
                    ↓ 调用
┌───────────────────────────────────────────────────────────┐
│  useActionCoreData({                                      │
│    tokenAddress: '0x123...',                              │
│    actionId: 5n,                                          │
│    account: '0xabc...'                                    │
│  })                                                       │
└───────────────────────────────────────────────────────────┘
                    ↓ 返回数据
┌───────────────────────────────────────────────────────────┐
│  {                                                        │
│    actionInfo: {...},                                     │
│    participantCount: 50n,                                 │
│    totalAmount: 500000n,                                  │
│    userJoinedAmount: 10000n,                              │
│    isJoined: true,                                        │
│    isExtensionAction: true,                               │
│    extensionAddress: '0x742...',                          │
│    currentRound: 10n,                                     │
│  }                                                        │
└───────────────────────────────────────────────────────────┘
                    ↓ 条件渲染
┌───────────────────────────────────────────────────────────┐
│  JSX 渲染                                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━│
│                                                           │
│  {isExtensionAction && (                                  │
│    <Badge variant="default">                             │
│      <Rocket /> 扩展行动                                  │
│    </Badge>                                               │
│  )}                                                       │
│                                                           │
│  <div>                                                    │
│    <p>参与人数: {participantCount.toString()}</p>         │
│    <p>参与总额: {formatAmount(totalAmount)}</p>           │
│                                                           │
│    {isJoined && (                                         │
│      <div>                                                │
│        <CheckCircle2 /> 已参与                            │
│        <p>我的参与: {formatAmount(userJoinedAmount)}</p>  │
│      </div>                                               │
│    )}                                                     │
│  </div>                                                   │
└───────────────────────────────────────────────────────────┘
```

## 🧩 Hook 依赖关系图

```
                 useActionCoreData (增强版)
                         │
          ┌──────────────┴──────────────┐
          │                             │
          ↓                             ↓
    Core 数据获取              useActionParticipationData
  (useReadContracts)                    │
    │                     ┌─────────────┼─────────────┐
    │                     ↓             ↓             ↓
    │                useExtension  useAction    useAction
    │                     │       ExtensionStats Extension
    │                     │             │       UserStatus
    │                     ↓             │             │
    │         ExtensionCenter.extension │             │
    │                     │             │             │
    │                     │             ↓             ↓
    │                     │    ExtensionStakeLp  ExtensionCenter
    │                     │    - accountsCount   - isAccountJoined
    │                     │    - joinedValue
    │                     │             │             │
    │                     │             ↓             ↓
    │                     │       ExtensionStakeLp    │
    │                     │    - joinedValueByAccount │
    │                     │             │             │
    └─────────────────────┴─────────────┴─────────────┘
                          ↓
                   整合后的数据
```

## 📈 性能对比图表

```
┌────────────────────────────────────────────────────────────┐
│  RPC 调用次数对比                                           │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  场景: 获取扩展行动的完整参与数据                            │
│                                                            │
│  优化前 (逐个调用):                                         │
│  ████████ 4 次 RPC                                         │
│                                                            │
│  优化后 (批量调用):                                         │
│  ████ 2 次 RPC                                             │
│                                                            │
│  节省: 50% ⚡                                              │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│  响应时间对比 (假设单次 RPC 100ms)                          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  优化前: ████████████████████ ~400ms                       │
│                                                            │
│  优化后: ██████████ ~200ms                                 │
│                                                            │
│  提升: 50% faster ⚡                                       │
└────────────────────────────────────────────────────────────┘
```

---

通过这套可视化流程图，您可以清晰地理解整个系统的数据流转、架构分层和性能优化策略。
