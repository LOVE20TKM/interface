/**
 * Group 扩展 Hooks
 *
 * 包含 Group 扩展的所有功能：
 * - useGroupActionDynamicTabs: 动态标签（链群管理）
 * - composite: 组合查询（通过 ./composite 导出）
 *
 * 注意：contracts 相关的 hooks 请直接从具体文件导入，例如：
 * - useExtensionGroupAction
 * - useGroupManager
 * - useGroupJoin
 * - useGroupVerify
 * - useExtensionGroupActionFactory
 */

export * from './useGroupActionDynamicTabs';

